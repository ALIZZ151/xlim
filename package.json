{
  "name": "xlimstore-frontend-final",
  "version": "3.0.0",
  "private": true,
  "description": "XLIMSTORE mini app storefront for Vercel.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node scripts/build.js && npx serve dist",
    "check": "node scripts/build.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {}
}
